/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['store.storeimages.cdn-apple.com'], // เพิ่ม hostname ที่ต้องการอนุญาต
    },
  };
  
  export default nextConfig;
  